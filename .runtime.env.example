# Secret runtime configuration. Copy to .runtime.env, chmod 600, never commit.
API_ID=
API_HASH=
PHONE_NUMBER=
# BOT_API_TOKEN=  # Optional bot alternative; user-account features are limited for bots.

# Cloudflare API token is deliberately NOT persisted here. Supply it only to
# scripts/cloudflare-provision.sh for the provisioning run, then revoke it.
