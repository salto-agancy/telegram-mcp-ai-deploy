# Telegram agent skills

These original, task-scoped skills reduce context use and keep write actions deliberate.
They are copied into both Codex (`.agents/skills`) and Claude Code (`.claude/skills`)
discovery locations by repository layout tests.
